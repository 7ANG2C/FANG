rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /environments/{environment}/{document=**} {
      allow read, write: if environment == 'sit' || environment == 'prod';
    }
  }
}
